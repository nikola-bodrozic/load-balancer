http {

    upstream all {
        server node1:4000;
        server node2:4001;
    }

    server {
        listen 8080;
        server_name localhost 127.0.0.1;         

        location / {
            proxy_pass http://all/;
        }
    }
}

events { }
